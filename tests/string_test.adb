procedure gcd is
   a: String := "hello";
   b: String;
begin
   b := "hello";

   if a = b then
      Put_Line("It works!");
   end if;

   if a /= b then
      Put_Line("It doesn't work!");
   end if;
end gcd;
